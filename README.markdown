So it turns out I'm bad at searching the git documentation.  This option has
already added to `git merge` (as I always thought it should).  Just use `git
merge --ff-only`.

This repo is now here purely for historical reasons.
